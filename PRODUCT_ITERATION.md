# 庄Sir的状态栏 产品迭代说明

## 产品判断

早期版本已经能工作，但它把多个系统指标直接串在菜单栏里。这个设计在信息完整性上是成立的，但在 macOS 右上角这个极窄空间里会带来两个问题：

- 菜单栏文字过长时，macOS 会在空间紧张时隐藏后面的状态项。
- 多个数字同时出现，反而削弱了“一眼判断”的效率。

后续版本的方向是：菜单栏负责最小信号，展开面板负责完整信息。

## 这次改动

- 菜单栏固定为短宽度，不再随着多个指标无限增长。
- 菜单栏只显示一个焦点指标，用户可在面板里切换。
- 默认焦点是内存，因为它最容易解释“电脑为什么卡”。
- 展开面板加入系统压力判断，让普通用户不用逐项理解每个数字。
- 指标卡片改为更高密度的矩阵，减少滚动和视觉噪音。
- 网速改为短格式，例如 `42K/s`、`1.3M/s`。

## 后续商业版建议

- 加入开机自动启动。
- 加入正式 Developer ID 签名与 Apple 公证。
- 加入温度、风扇、电池健康度。
- 支持菜单栏多种样式：图标、图标+数字、纯图标。
- 支持异常提醒，例如内存超过 85% 时改变菜单栏颜色。
