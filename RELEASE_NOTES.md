# PulseBar 0.2.1

PulseBar 的交互修复版本，重点优化菜单栏点击后的面板位置和整体尺寸。

## 这版包含

- 点击菜单栏后改用自定义浮层定位，不再依赖系统 `NSPopover` 自动摆放
- 面板强制从菜单栏下方留出间距，避免顶到屏幕最上方
- 面板尺寸从 `430 × 640` 压缩到 `348 × 480`
- 进一步压缩标题、压力区、指标卡片和设置区间距
- 保留 0.2.0 的单焦点短菜单栏设计

## 安装

下载 `PulseBar-0.2.1.zip`，解压后双击 `PulseBar.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `PulseBar.app`，选择“打开”。
