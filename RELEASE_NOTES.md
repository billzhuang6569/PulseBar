# 庄Sir的状态栏 0.4.3

这是一次网速详情优化版本，重点让“谁正在占用网络”更清楚。

## 这版包含

- 网速详情行优先显示每个进程当前下载和上传速度
- `0KB` 会显示为 `0KB`，不再显示为 `Zero KB`
- 累计流量改为辅助信息，并明确标注为“累计”
- PID 降为更弱的辅助信息
- 修复网速文本重复拼接 `/s` 导致的 `KB/s/s` 问题
- 保留 0.4.2 的圆角悬浮窗口与详情窗口视觉修复

## 安装

下载 GitHub 附件 `ZhuangSir-StatusBar-0.4.3.zip`，解压后双击 `庄Sir的状态栏.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `庄Sir的状态栏.app`，选择“打开”。
