# PulseBar 0.2.3

PulseBar 的网速显示优化版本，重点改进菜单栏专注网速时的信息表达。

## 这版包含

- 菜单栏专注“网速”时改为两行显示
- 第一行显示 `↑` 上传速度
- 第二行显示 `↓` 下载速度
- 不再显示上传 + 下载的总速度
- 使用自绘状态栏控件，让两行数字在菜单栏高度内稳定显示

## 安装

下载 `PulseBar-0.2.3.zip`，解压后双击 `PulseBar.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `PulseBar.app`，选择“打开”。
