# 庄Sir的状态栏 0.4.1

这是一次主面板裁切修复版本，重点解决 0.4.0 新增横向卡片后窗口高度不足，导致顶部标题和底部按钮被裁掉的问题。

## 这版包含

- 主面板高度从旧版尺寸调整为适配 0.4.0 新结构的完整高度
- 顶部标题、系统压力区、中间横向卡片、设置区和底部按钮现在都能完整显示
- 状态栏图标和文字使用同一套自适应颜色，随 macOS 菜单栏外观切换纯白或纯黑
- 降低状态栏数字字号，网速双行显示更轻、更协调
- 中间大卡片改为横向滚动，只要开启了某项检测，就会出现对应卡片
- 右侧增加悬浮箭头，可以横向切换更多卡片
- 点击大卡片会在主面板旁边弹出详情窗口
- 网速详情显示进程网络排行，按当前活动或累计流量排序
- 内存详情显示进程内存占用排行
- CPU 详情显示进程 CPU 占用排行
- 硬盘详情显示常用目录占用排行
- 电池详情显示高 CPU 进程作为耗电线索

## 说明

硬盘和电池的系统级“按 App 精确排行”在 macOS 上需要更高权限或更复杂的系统接口。这版优先选择稳定、无需额外权限、普通团队成员可以直接打开使用的方案。

## 安装

下载 GitHub 附件 `ZhuangSir-StatusBar-0.4.1.zip`，解压后双击 `庄Sir的状态栏.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `庄Sir的状态栏.app`，选择“打开”。
