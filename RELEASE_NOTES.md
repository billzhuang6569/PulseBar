# 庄Sir的状态栏 0.3.0

品牌化版本，产品名正式改为“庄Sir的状态栏”，并加入可显示在 Finder 与 App 包里的 macOS 图标。

## 这版包含

- 产品显示名改为“庄Sir的状态栏”
- 新增 macOS App 图标
- Finder 中的 `.app` 名称改为 `庄Sir的状态栏.app`
- 分享包名称改为 `庄Sir的状态栏-0.3.0.zip`
- 保留原有菜单栏监控、详情面板、网速上下行分离显示

## 安装

下载 `庄Sir的状态栏-0.3.0.zip`，解压后双击 `庄Sir的状态栏.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `庄Sir的状态栏.app`，选择“打开”。
