# 庄Sir的状态栏 0.4.4

这是一次网速进程排行修复版本，解决详情面板中所有进程实时速度长期显示为 0 的问题。

## 这版包含

- 每次刷新会在同一次系统采样中取得累计帧和 1 秒实时增量帧，避免基线丢失
- 进程排行只展示当前采样周期真正产生流量的进程
- 严格按下载与上传的合计实时速度从高到低排序
- 无活动流量时明确显示“当前采样周期没有进程使用网络”，不再用累计流量伪装实时排行
- 累计流量继续作为辅助信息保留，PID 保持弱化显示
- 增加网速采样解析测试，覆盖实时增量、空闲进程和不完整采样

## 安装

下载 GitHub 附件 `ZhuangSir-StatusBar-0.4.4.zip`，解压后双击 `庄Sir的状态栏.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `庄Sir的状态栏.app`，选择“打开”。
