# 庄Sir的状态栏 0.3.3

这是一次状态栏可读性修复版本，重点解决深色桌面背景下黑色状态栏文字和图标不够清晰的问题。

## 这版包含

- 状态栏图标和文字会跟随 macOS 当前菜单栏外观自动切换白色或黑色
- 深色桌面背景上，状态栏内容会显示为白色，和其他菜单栏 App 保持一致
- 浅色菜单栏上，状态栏内容仍会使用深色，保证对比度
- 外观变化时会自动重绘，不需要重启 App
- 保留网速在状态栏中的地球图标，以及上传、下载双行显示
- 保留 0.3.2 的深色主面板和 5 个快速切换按钮

## 安装

下载 GitHub 附件 `ZhuangSir-StatusBar-0.3.3.zip`，解压后双击 `庄Sir的状态栏.app`。

如果 macOS 第一次提示无法确认开发者，请右键点击 `庄Sir的状态栏.app`，选择“打开”。
